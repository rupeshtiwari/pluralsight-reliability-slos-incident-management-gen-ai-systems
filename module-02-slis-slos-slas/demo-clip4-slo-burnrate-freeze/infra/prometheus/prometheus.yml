global:
  scrape_interval: 2s
  evaluation_interval: 2s

rule_files:
  - /etc/prometheus/recording.yml
  - /etc/prometheus/rules.yml

scrape_configs:
  - job_name: "genai-slo-demo"
    metrics_path: /metrics
    static_configs:
      - targets: ["host.docker.internal:8080"]
